'use client'
import{useEffect,useMemo,useState}from'react';import{useRouter}from'next/navigation';import AppShell from'../../components/app-shell';import{createClient}from'../../supabase-client'
type Lead={id:string;status:string;responsavel_id:string|null;created_at:string};type P={id:string;nome:string;role:string};type F={id:string;assigned_to:string;status:string;due_at:string}
export default function DashboardGerencial(){const router=useRouter();const supabase=useMemo(()=>createClient(),[]);const[role,setRole]=useState('corretor');const[leads,setLeads]=useState<Lead[]>([]);const[people,setPeople]=useState<P[]>([]);const[followups,setFollowups]=useState<F[]>([]);useEffect(()=>{(async()=>{const{data:{user}}=await supabase.auth.getUser();if(!user){router.replace('/login');return}const{data:p}=await supabase.from('profiles').select('role').eq('id',user.id).maybeSingle();setRole(p?.role??'corretor');const[{data:l},{data:ps},{data:f}]=await Promise.all([supabase.from('leads').select('id,status,responsavel_id,created_at').is('deleted_at',null).limit(5000),supabase.from('profiles').select('id,nome,role').order('nome'),supabase.from('lead_followups').select('id,assigned_to,status,due_at').limit(5000)]);setLeads((l??[])as Lead[]);setPeople((ps??[])as P[]);setFollowups((f??[])as F[])})()},[]);if(role==='corretor')return <AppShell role={role}><div className="page"><section className="card"><strong>Acesso restrito</strong><p className="sub">O dashboard gerencial é exclusivo para gerente e administrador.</p></section></div></AppShell>;const won=leads.filter(l=>l.status==='ganho').length;const pending=followups.filter(f=>f.status==='pendente').length;const overdue=followups.filter(f=>f.status==='pendente'&&new Date(f.due_at)<new Date()).length;const brokers=people.filter(p=>p.role==='corretor');return <AppShell role={role}><div className="page"><div className="eyebrow">Gestão</div><h1>Dashboard Gerencial</h1><p className="sub">Visão rápida da operação, carteira e produtividade da equipe.</p><section className="grid"><div className="card"><div className="metric-label">Leads ativos</div><div className="metric">{leads.length}</div></div><div className="card"><div className="metric-label">Ganhos</div><div className="metric">{won}</div></div><div className="card"><div className="metric-label">Follow-ups pendentes</div><div className="metric">{pending}</div></div><div className="card"><div className="metric-label">Follow-ups atrasados</div><div className="metric">{overdue}</div></div></section><section className="card"><h2 className="section-title">Carteira por corretor</h2>{brokers.map(p=><div className="detail-row" key={p.id}><span>{p.nome}</span><strong>{leads.filter(l=>l.responsavel_id===p.id).length} leads</strong></div>)}{brokers.length===0&&<div className="empty">Nenhum corretor cadastrado.</div>}</section><section className="card"><h2 className="section-title">Funil atual</h2>{['novo','em_atendimento','visita','proposta','ganho','perdido'].map(s=><div className="detail-row" key={s}><span>{s.replace('_',' ')}</span><strong>{leads.filter(l=>l.status===s).length}</strong></div>)}</section></div></AppShell>}
