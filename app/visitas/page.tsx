'use client'
import { FormEvent,useEffect,useMemo,useState } from 'react'
import { useRouter } from 'next/navigation'
import AppShell from '../../components/app-shell'; import { createClient } from '../../supabase-client'
type Lead={id:string;nome:string};type Visit={id:string;lead_id:string;property_id:string|null;assigned_to:string;scheduled_at:string;status:string;notes:string|null;result:string|null};type Profile={id:string;nome:string};type Property={id:string;nome:string;empreendimento:string|null;unidade:string|null}
const labels:Record<string,string>={agendada:'Agendada',confirmada:'Confirmada',realizada:'Realizada',cancelada:'Cancelada',nao_compareceu:'Não compareceu'}
export default function VisitasPage(){const router=useRouter();const supabase=useMemo(()=>createClient(),[]);const[role,setRole]=useState('corretor');const[userId,setUserId]=useState('');const[visits,setVisits]=useState<Visit[]>([]);const[leads,setLeads]=useState<Lead[]>([]);const[profiles,setProfiles]=useState<Profile[]>([]);const[properties,setProperties]=useState<Property[]>([]);const[show,setShow]=useState(false);const[form,setForm]=useState({lead_id:'',property_id:'',assigned_to:'',scheduled_at:'',notes:''});const[error,setError]=useState('');const[saving,setSaving]=useState(false);const load=async()=>{const{data:{user}}=await supabase.auth.getUser();if(!user){router.replace('/login');return}setUserId(user.id);const{data:p}=await supabase.from('profiles').select('role').eq('id',user.id).maybeSingle();setRole(p?.role??'corretor');const[{data:v},{data:l},{data:people},{data:props}]=await Promise.all([supabase.from('visits').select('*').order('scheduled_at',{ascending:true}).limit(1000),supabase.from('leads').select('id,nome').is('deleted_at',null).order('nome').limit(2000),supabase.from('profiles').select('id,nome').order('nome'),supabase.from('properties').select('id,nome,empreendimento,unidade').order('nome').limit(2000)]);setVisits((v??[]) as Visit[]);setLeads((l??[]) as Lead[]);setProfiles((people??[]) as Profile[]);setProperties((props??[]) as Property[])};useEffect(()=>{load()},[]);const name=(id:string)=>leads.find(x=>x.id===id)?.nome??'Lead';const person=(id:string)=>profiles.find(x=>x.id===id)?.nome??'Corretor';const property=(id:string|null)=>properties.find(x=>x.id===id)?.nome??'Sem imóvel';function open(){setForm({lead_id:leads[0]?.id??'',property_id:'',assigned_to:userId,scheduled_at:'',notes:''});setShow(true);setError('')}async function save(e:FormEvent){e.preventDefault();setSaving(true);const{error}=await supabase.from('visits').insert({lead_id:form.lead_id,property_id:form.property_id||null,assigned_to:form.assigned_to||userId,scheduled_at:new Date(form.scheduled_at).toISOString(),notes:form.notes||null,created_by:userId});if(error)setError(error.message);else{setShow(false);await load()}setSaving(false)}async function status(id:string,status:string){setSaving(true);const{error}=await supabase.from('visits').update({status}).eq('id',id);if(error)setError(error.message);else await load();setSaving(false)}return <AppShell role={role}><div className="page"><div className="eyebrow">Agenda comercial</div><h1>Visitas</h1><p className="sub">Agende, confirme e acompanhe as visitas dos seus leads.</p><div className="toolbar"><button className="btn primary" onClick={open}>+ Nova visita</button></div>{error&&<div className="form-error">{error}</div>}<section className="card"><div className="table-wrap"><table><thead><tr><th>Data</th><th>Lead</th><th>Imóvel</th><th>Responsável</th><th>Status</th><th>Ações</th></tr></thead><tbody>{visits.length?visits.map(v=><tr key={v.id}><td>{new Date(v.scheduled_at).toLocaleString('pt-BR')}</td><td><strong>{name(v.lead_id)}</strong></td><td>{property(v.property_id)}</td><td>{person(v.assigned_to)}</td><td><span className="status blue">{labels[v.status]??v.status}</span></td><td><select value={v.status} disabled={saving} onChange={e=>status(v.id,e.target.value)}>{Object.entries(labels).map(([k,l])=><option key={k} value={k}>{l}</option>)}</select></td></tr>):<tr><td colSpan={6}><div className="empty">Nenhuma visita cadastrada.</div></td></tr>}</tbody></table></div></section></div>{show&&<div className="modal-backdrop"><div className="modal"><div className="modal-header"><h2>Nova visita</h2><button className="icon-btn" onClick={()=>setShow(false)}>×</button></div><form className="lead-form" onSubmit={save}><label>Lead<select value={form.lead_id} onChange={e=>setForm({...form,lead_id:e.target.value})}>{leads.map(l=><option key={l.id} value={l.id}>{l.nome}</option>)}</select></label><label>Imóvel<select value={form.property_id} onChange={e=>setForm({...form,property_id:e.target.value})}><option value="">Sem imóvel</option>{properties.map(p=><option key={p.id} value={p.id}>{p.nome}</option>)}</select></label><label>Responsável<select value={form.assigned_to} onChange={e=>setForm({...form,assigned_to:e.target.value})}>{profiles.filter(p=>p.id===userId||role!=='corretor').map(p=><option key={p.id} value={p.id}>{p.nome}</option>)}</select></label><label>Data e hora<input type="datetime-local" required value={form.scheduled_at} onChange={e=>setForm({...form,scheduled_at:e.target.value})}/></label><label>Observações<textarea rows={4} value={form.notes} onChange={e=>setForm({...form,notes:e.target.value})}/></label><div className="modal-actions"><button type="button" className="btn" onClick={()=>setShow(false)}>Cancelar</button><button className="btn primary" disabled={saving}>{saving?'Salvando...':'Agendar'}</button></div></form></div></div>}</AppShell>}
